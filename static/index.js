// here we will need to make a POST request via fetch
// we will send in the submitted image, and get back the classification results
// we should then display the classificaiton results to the user

// add the listener to the button w the value predict

// document.addEventListener("DOMContentLoaded", () => {
//     predictBtn = document.querySelector('#predict_button')
//     imgInput = document.querySelector('#image_file')


//     predictBtn.addEventListener("click", () => {
//         console.log('did it work')
//         if (imgInput.value === "") {
//             alert("Please submit an image.")
//         }
//         else {
//             console.log(imgInput.value)
//         }
//     })
// })